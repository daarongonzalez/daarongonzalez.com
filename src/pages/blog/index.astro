---
import Layout from "@/layouts/Layout.astro";
import FeaturedBlogCard from "@/components/FeaturedBlogCard";
import BlogFilter from "@/components/BlogFilter";
import { getAllPosts } from "@/lib/sanity";

const posts = await getAllPosts();
const featuredPost = posts.find((post) => post.featured) ?? posts[0];
const gridPosts = posts.filter((post) => post._id !== featuredPost?._id);
---

<Layout title="Blog" description="Thoughts on AI strategy, education, and building without shortcuts.">
  <section class="mt-5">
    <div class="hd text-sm text-brand-base">Blog</div>
    <div class="hd mt-1.5 text-[40px]">Notes &amp; Field Guides</div>
    <p class="m-0 mt-2 max-w-[560px] text-base text-ink-dark">
      Thoughts on AI strategy, education, and building without shortcuts.
    </p>
  </section>

  {featuredPost && (
    <section class="mt-7">
      <FeaturedBlogCard post={featuredPost} />
    </section>
  )}

  <section class="mt-6">
    <BlogFilter posts={gridPosts} client:load />
  </section>

  {posts.length === 0 && (
    <p class="mt-6 text-center text-ink-base">No posts published yet — check back soon.</p>
  )}
</Layout>
